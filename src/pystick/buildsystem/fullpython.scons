Import("env libfullpython freeze_modules")
env = env.Clone()
env.VariantDir("python", "#python", duplicate=False)  # FIXME create a #python_build variant in the root and use that

all_objects = ['python/Modules/python.c', 'modules/config.c'] + env['PYTHON_MODULES'] + libfullpython
fullpython = env.Program('python.full', all_objects)

Return('fullpython')